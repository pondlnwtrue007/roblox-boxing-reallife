# mediapipe ดึง opencv-contrib-python + numpy + pillow มาให้เอง จึงไม่ระบุซ้ำ (กันติดตั้งซ้อน)
mediapipe>=0.10.14
pydirectinput>=1.0.4
pygrabber>=0.2          # ดึงรายชื่อกล้อง (แอป GUI)
pillow>=9              # แสดงภาพกล้องในหน้าต่างแอป (PIL)
customtkinter>=5.2     # หน้าตาแอป GUI ธีมมืดโมเดิร์น (app.py)
# tkinter เป็น standard library ของ Python (ไม่ต้องลงเพิ่ม)

# --- สำหรับ build เป็น .exe เท่านั้น (ไม่จำเป็นตอนรันปกติ) ---
# pyinstaller>=6.0
